#include "rayTracer.h"

int main(int argc, char* args[]) {
	Application app(400, 400);
	app.start();

	//auto im = Image("tex.jpg");
	//for (int i = 0; i<875; i++)
	//	im.pixelColor({ i,0 });
	return 0;
}